/* Quantum Health Biotech Park — static navigation.
   Every link performs a standard full-page load, so stylesheets and
   page scripts always load fresh and navigation never breaks. */
(function () {
  'use strict';
})();